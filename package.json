{
  "name": "@powerkernel/node-crud-gen",
  "version": "0.1.8",
  "description": "CLI CRUD generator for Power Kernel projects",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "node-crud-gen": "lib/index.js"
  },
  "files": [
    "lib",
    "templates"
  ],
  "scripts": {
    "dev": "npm run clean && tsc && node lib/index.js Post",
    "release": "npx standard-version && git push --follow-tags origin main",
    "clean": "del ./lib/* && del ./src/domains && del ./src/repositories",
    "build": "npm run clean && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "twing": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "del-cli": "^4.0.1",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3",
    "typescript": "^4.6.2"
  }
}

import { Entity, Jsonifier } from "@powerkernel/power-common";
import { {{EntityDto}} } from "../dtos";

/**
 * class {{Class}}
 */
class {{Class}} extends Entity implements Jsonifier<{{EntityDto}}> {
  // private attributes
  

  // constructor
  constructor(dto: {{EntityDto}}) {
    super();
    this.id = dto.id;
    this.createdAt = dto.createdAt;
    this.updatedAt = dto.updatedAt;
  }

  // setters/getters
  {{SetGetters}}


  // factory method
  public static create(dto: {{EntityDto}}) {
    return new {{Class}}(dto);
  }

  // to json
  jsonify() {
    return {
      id: this.id,
      createdAt: this.createdAt,
      updatedAt: this.updatedAt,
    };
  }
}

export default {{Class}};
